<h1>Your Cart</h1>

<ul>
  <% @cart.each do |listing| %>
    <li>Item: <%= listing.title %> <%= number_to_currency(listing.price) %></li>
  <% end %>
</ul>

<%= link_to "Proceed to checkout", "", id: "stripe", class: 'btn btn-primary' %>

<%= javascript_pack_tag "stripe" %>

